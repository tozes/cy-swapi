context('Star Wars API tests', () => {
  before(() => {
    // First we need to open the page in the right domain (cypress doesn't support cross domain testing)
    cy.visit('/'); 
  });

  it('tests something', () => {

  });
})
